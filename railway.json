
{
  "deploy": {
    "startCommand": "python TestDS3.py",
    "restartPolicyType": "ON_FAILURE",
    "startTimeout": 120,
    "variables": {
      "BOT_TOKEN": "${{BOT_TOKEN}}",
      "DEEPSEEK_API_KEY": "${{DEEPSEEK_API_KEY}}",
      "OPENAI_API_KEY": "${{OPENAI_API_KEY}}"
    }
  },
  "build": {
    "nixpacks": {
      "packages": ["poppler-utils", "libjpeg", "zlib"]
    }
  }
}
